mod dotenv;
use std::sting;
use std::collections::HashMap;


const DEFAULT_CONFIG_FILE: String = ".env";

struct DotEnv{
    config_setting: HashMap<String, String>,
}



impl DotEnv{
    fn new(&self){
        config_setting = HashMap::new();

        // load configfile
    }
}
